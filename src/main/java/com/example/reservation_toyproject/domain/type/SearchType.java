package com.example.reservation_toyproject.domain.type;

/**
 * 환자 이름
 * 이메일
 * 전화번호
 * 생년월일
 * 병원이름
 * 진료항목
 * 접수상태
 */
public enum SearchType {
    USERNAME, EMAIL, PHONENUMBER, BIRTHDAY, HOSPITALNAME, TXLIST, RESERVATIONSTATUS
}
